# Core — all pure Python, no Rust/compilation needed
requests>=2.31.0
python-dotenv>=1.0.0

# yt-dlp: install via pkg, NOT pip (avoids Rust build issues on Android)
# Run this in Termux:  pkg install yt-dlp
